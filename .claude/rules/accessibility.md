# Accessibility Rules

**Trigger**: `**/*.tsx`

## ARIA
- Interactive elements must have `aria-label` or visible label.
- Images must have `alt` text.
- Use semantic HTML (`<button>`, `<nav>`, `<main>`) over `<div>` where possible.
